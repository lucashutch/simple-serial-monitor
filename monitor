#!/usr/bin/env python3

import sys
import os

# Add src to path to import package
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

# Import the main function that handles argument parsing and execution
from embedded_cereal_bowl.monitor.monitor import main

if __name__ == "__main__":
    main()